package Dynamic;

public class TargetSum {
    public static void main(String[] args) {
        // given an array, assign sign(+ or -) before every element so that the expression evaluates to given sum.
        // input = [1 1 2 3], sum = 1
        // output (-1+1-2+3), (1-1-2+3), {1+1+2-3}. Result = 3
        
        // this problem can be reduced to "Count of number of subsets with give difference"
        // negatives will be in one subset and positives in another subset.
    }
}
